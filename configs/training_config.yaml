training:
  dataset_path: data/motorcycle_rider_dataset
  train_split: train
  val_split: valid
  epochs: 30
  batch_size: 4
  learning_rate: 0.0001
  weight_decay: 0.00005
  warmup_steps: 1000
  scheduler: cosine
  num_workers: 4
  save_dir: checkpoints/
  log_interval: 20
  eval_interval: 1
  resume_checkpoint: null
